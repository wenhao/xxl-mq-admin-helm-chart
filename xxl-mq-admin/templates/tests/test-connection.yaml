apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xxl-mq-admin.fullname" . }}-test-connection"
  labels:
    {{- include "xxl-mq-admin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xxl-mq-admin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
